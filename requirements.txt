# Runtime dependencies for the Streamlit app.
# Streamlit Community Cloud installs exactly these.
# Pinned to the versions the model was trained/pickled with so unpickling is
# consistent on the host. For notebook/training work, see requirements-dev.txt.
streamlit==1.57.0
scikit-learn==1.8.0
lightgbm==4.6.0
joblib==1.5.3
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
tldextract==5.3.1
